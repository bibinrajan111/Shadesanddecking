# Brand assets required before visual build

The supplied repository does not currently contain the official company logo or poster. Add the original, unmodified files here before Phase 3:

- `logo.*` — the official company logo, used directly by the navigation and footer.
- `company-poster.*` — used only as a private content/visual reference; do not render it as a web page.
- Optimised owned project photography for services and project features.

Do not substitute or redraw the logo. Update the service image values in `src/data/services.ts` once approved project imagery is available.
