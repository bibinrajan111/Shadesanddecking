import { motion, useReducedMotion } from 'framer-motion'
import { imageReveal, staggerContainer } from '../../animations/variants'
import { featuredProjects } from '../../data/projects'
import { Container } from '../ui/Container'
import { SectionHeading } from '../ui/SectionHeading'
import { ProjectPlaceholder } from './ProjectPlaceholder'
export function ProjectsPreview() { const reduced = useReducedMotion(); return <section className="bg-warm-grey/45 py-16 md:py-24 lg:py-28"><Container><SectionHeading eyebrow="Featured projects" heading="Designed to belong in the landscape." description="A place for future Shades and Decking project photography—presented as a considered portfolio, not a generic gallery." /><motion.div className="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-12" variants={staggerContainer(reduced)} initial="hidden" whileInView="visible" viewport={{ once: true, amount: 0.16 }}>{featuredProjects.map((project, index) => <motion.article key={project.id} variants={imageReveal(reduced)} className={index === 0 ? 'md:col-span-2 lg:col-span-7' : index === 1 ? 'lg:col-span-5' : 'lg:col-span-6'}><div className="group relative overflow-hidden bg-charcoal">{project.image ? <img src={project.image} alt={project.imageAlt} loading="lazy" width="1200" height="900" className={`w-full object-cover ${project.aspect === 'portrait' ? 'aspect-[4/5]' : project.aspect === 'square' ? 'aspect-square' : 'aspect-[16/10]'}`} /> : <ProjectPlaceholder label={project.imageAlt} className={project.aspect === 'portrait' ? 'aspect-[4/5]' : project.aspect === 'square' ? 'aspect-square' : 'aspect-[16/10]'} />}<div className="absolute inset-x-0 bottom-0 flex items-end justify-between bg-gradient-to-t from-deep-charcoal/80 to-transparent p-5 text-white"><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-bronze">{project.category}</p><h3 className="mt-1 text-xl font-medium">{project.title}</h3></div><span aria-hidden="true" className="text-xl transition-transform duration-300 group-hover:translate-x-1">↗</span></div></div></motion.article>)}</motion.div></Container></section> }
