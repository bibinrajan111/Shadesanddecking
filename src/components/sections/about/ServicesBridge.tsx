import { Button } from '../../ui/Button'
import { Container } from '../../ui/Container'
import { services } from '../../../data/services'
export function ServicesBridge() { return <section className="bg-white py-16 md:py-24"><Container><div className="flex flex-col gap-8 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-timber">What we create</p><h2 className="mt-5 max-w-xl text-3xl font-semibold tracking-[-.04em] text-charcoal sm:text-4xl">Explore solutions for the way you live outdoors.</h2></div><Button to="/services" variant="outline">Explore Our Services</Button></div><div className="mt-10 grid gap-x-8 border-t border-warm-grey sm:grid-cols-2 lg:grid-cols-3">{services.slice(0, 3).map((service) => <div className="border-b border-warm-grey py-5" key={service.id}><h3 className="text-lg font-semibold text-charcoal">{service.title}</h3><p className="mt-2 text-sm leading-6 text-charcoal/70">{service.shortDescription}</p></div>)}</div></Container></section> }
