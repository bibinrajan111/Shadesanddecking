import { motion, useReducedMotion } from 'framer-motion'
import { imageReveal, revealUp } from '../../../animations/variants'
import { Button } from '../../ui/Button'
import { Container } from '../../ui/Container'
import { ProjectPlaceholder } from '../ProjectPlaceholder'
export function ServicesHero() { const reduced=useReducedMotion(); return <section className="bg-charcoal text-white"><Container><div className="grid items-end gap-8 py-14 md:py-20 lg:grid-cols-12"><motion.div initial="hidden" animate="visible" className="lg:col-span-7"><motion.p variants={revealUp(reduced)} className="text-xs font-bold uppercase tracking-[.2em] text-bronze">What we create</motion.p><motion.h1 variants={revealUp(reduced,.08)} className="mt-5 text-5xl font-semibold leading-[.94] tracking-[-.055em] sm:text-6xl lg:text-7xl">Outdoor spaces designed for living.</motion.h1><motion.p variants={revealUp(reduced,.16)} className="mt-7 max-w-xl text-lg leading-8 text-white/75">Tailored pergolas, decking, screens, fencing and outdoor living solutions for New Zealand homes.</motion.p><motion.div variants={revealUp(reduced,.22)} className="mt-8"><Button to="/contact">Get a Free Consultation &amp; Quote</Button></motion.div></motion.div><motion.div initial="hidden" animate="visible" variants={imageReveal(reduced)} className="lg:col-span-4 lg:col-start-9"><ProjectPlaceholder label="Services hero photography placeholder" className="aspect-[5/4]" /></motion.div></div></Container></section> }
