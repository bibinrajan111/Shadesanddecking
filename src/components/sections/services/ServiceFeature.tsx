import { AnimatedSection } from '../../ui/AnimatedSection'
import { Button } from '../../ui/Button'
import { Section } from '../../ui/Section'
import type { Service } from '../../../data/services'
import { ProjectPlaceholder } from '../ProjectPlaceholder'
type Props={service:Service; index:number}
export function ServiceFeature({service,index}:Props){const reverse=index%2===1; const dark=index===3; return <Section className={dark?'bg-deep-charcoal text-white':''}><div className={`grid items-center gap-9 lg:grid-cols-12 ${reverse?'':' '}`}><AnimatedSection className={`${reverse?'lg:order-2 lg:col-span-6':'lg:col-span-6'}`}><ProjectPlaceholder label={service.imageAlt} className="aspect-[5/4]" /></AnimatedSection><AnimatedSection className={`${reverse?'lg:order-1 lg:col-span-5':'lg:col-span-5 lg:col-start-8'}`}><p className="text-xs font-bold uppercase tracking-[.2em] text-timber">0{index+1} — Service</p><h2 className={`mt-4 text-3xl font-semibold tracking-[-.04em] sm:text-4xl ${dark?'text-white':'text-charcoal'}`}>{service.title}</h2><p className={`mt-5 text-lg leading-8 ${dark?'text-white/75':'text-charcoal/75'}`}>{service.fullDescription}</p><ul className={`mt-7 space-y-3 border-t ${dark?'border-white/20 text-white/80':'border-warm-grey text-charcoal/75'}`}>{service.benefits.map(item=><li key={item} className="border-b border-inherit py-3">{item}</li>)}</ul><Button to="/contact" variant={dark?'outline':'primary'} className={dark?'mt-8 border-white text-white hover:bg-white hover:text-charcoal':'mt-8'}>{service.callToAction}</Button></AnimatedSection></div></Section>}
