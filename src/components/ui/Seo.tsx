import { useEffect } from 'react'
import { useLocation } from 'react-router-dom'
import { company } from '../../data/company'
import { seoByPath } from '../../data/seo'
function setMeta(attribute:'name'|'property',key:string,content:string){let node=document.querySelector<HTMLMetaElement>(`meta[${attribute}="${key}"]`);if(!node){node=document.createElement('meta');node.setAttribute(attribute,key);document.head.appendChild(node)}node.content=content}
export function Seo(){const {pathname}=useLocation();useEffect(()=>{const seo=seoByPath[pathname]??seoByPath['/'];const canonical=company.siteUrl?`${company.siteUrl}${seo.path}`:undefined;document.title=seo.title;const canonicalLink=document.querySelector<HTMLLinkElement>('link[rel="canonical"]');if(canonical){const link=canonicalLink??document.createElement('link');if(!canonicalLink){link.rel='canonical';document.head.appendChild(link)}link.href=canonical}else canonicalLink?.remove();setMeta('name','description',seo.description);setMeta('property','og:title',seo.title);setMeta('property','og:description',seo.description);setMeta('property','og:type','website');if(canonical)setMeta('property','og:url',canonical);setMeta('name','twitter:card',seo.ogImage?'summary_large_image':'summary');if(seo.ogImage)setMeta('property','og:image',seo.ogImage)},[pathname]);return null}
